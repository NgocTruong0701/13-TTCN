@model IEnumerable<QLThuVien.Models.QUANTRIVIEN>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenQTV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.taiKhoan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.matKhau)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenQTV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.taiKhoan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.matKhau)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.maQTV }) |
            @Html.ActionLink("Details", "Details", new { id=item.maQTV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.maQTV })
        </td>
    </tr>
}

</table>
